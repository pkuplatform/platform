.gogo.card
  %span.avatar
    = image_tag current_user.profile.avatar.url(:small)
  .info
    .name
      = current_user.name
    .nickname
      = current_user.nickname.truncate(15)
    .points
      = t :level, :points => current_user.profile.points
