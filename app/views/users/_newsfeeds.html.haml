.newsfeeds.box
  .box-header
    = t "newsfeed"
  .box-body
    - if @newsfeeds.empty?
      - Event.last(50).each do |event|
        = render :partial => "newsfeed", :locals => { :event => event }
    - else
      - @newsfeeds.each do |newsfeed|
        = render :partial => "newsfeed", :locals => { :event => newsfeed.event }
  .more
