.hot.groups
  .title= t '.title'
  %ul
    - Group.hot.each do |group|
      %li
        = link_to (image_tag group.thumb), group
        .meta
          .name= link_to group.name.truncate(20), group
          .points= group.points
