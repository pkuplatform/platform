.recommend.groups
  .title= t '.title'
  %ul
    - Group.recommend.each do |group|
      %li
        = link_to (image_tag group.logo.url(:thumb)), group
        .meta
          .name= link_to group.name.truncate(20), group
          .points= group.points
