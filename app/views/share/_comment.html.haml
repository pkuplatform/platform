%li
  .span-2
    = image_tag comment.user.avatar
  .span-12.last
    .comment-title  
      = link_to comment.user.name, comment.user
      = " / #{comment.created_at}"
    .comment-body
      = comment.body
  .clear