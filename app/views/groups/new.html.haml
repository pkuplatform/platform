.page-header
  %h1= t '.title'
= simple_form_for @group, :html => {:class => 'form-horizontal'} do |f|
  = f.input :name
  = f.input :slogan
  = f.input :email
  = f.input :founded_at, :as => :date
  = f.input :logo, :as => :file
  = f.button :submit
