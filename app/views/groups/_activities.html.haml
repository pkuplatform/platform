.activities
  .header
    %span= t :activity
    = link_to (t :activity_more), activities_group_path if @more
  .list
    - if @group.activities.empty?
      .box
        .warn= t '.warn'
    - else
      = render :partial => 'activities/card', :collection => @group.activities.last(3), :as => :activity
