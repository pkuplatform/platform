= render :partial => 'share/breadcrumbs'
.page-header
  %h1= t '.header'
.description
  = sanitize(@group.description, :attributes => %w(id class src style href _cke_saved_href title alt width height))  
