.news.label.label-info
  = subject.name.truncate(10)
  = t '.action', :action => t(event.action)
  = t '.type', :holder => t(event.object_type)
  %span.time
    = t '.time', :holder => distance_of_time_in_words_to_now(event.created_at)
